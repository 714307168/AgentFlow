{
  "name": "agentflow-desktop",
  "version": "1.1.138",
  "description": "Desktop agent for AgentFlow remote control",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs');const path=require('path');for (const name of ['terminal.js','terminal.js.map','project-runtime-rules.js','project-runtime-rules.js.map']) { const source=path.join('dist','renderer',name); if (fs.existsSync(source)) { fs.copyFileSync(source,path.join('renderer',name)); } }\"",
    "test": "npm run build && node --test test/*.test.js",
    "start": "electron dist/src/main.js",
    "dev": "tsc --watch",
    "rebuild": "electron-rebuild",
    "generate-icons": "powershell -ExecutionPolicy Bypass -File .\\scripts\\generate-icons.ps1",
    "dist": "npm run generate-icons && electron-builder --publish never",
    "dist:win": "npm run generate-icons && electron-builder --win nsis --publish never"
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "node-pty": "^1.0.0",
    "uuid": "^9.0.0",
    "ws": "^8.16.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "@types/ws": "^8.5.10",
    "electron": "^29.0.0",
    "electron-builder": "^24.9.1",
    "electron-rebuild": "^3.2.9",
    "typescript": "^5.3.3"
  }
}
